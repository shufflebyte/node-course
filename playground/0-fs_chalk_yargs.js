// comes from node
const fs = require('fs')

// installed via npm
const validator = require('validator');
const chalk = require('chalk');
const yargs = require('yargs');

//const yargsParser = require('yargs-parser');

// // fs.writeFileSync('notes.txt', 'yoyoyo');
// fs.appendFileSync('notes.txt', 'some other yoyo');
// fs.appendFileSync('notes.txt', myname);
// console.log(notes.getNotes());
// console.log(validator.isEmail('hugo@horst.de'));

// // chalk is for colored console logs...
// console.log(chalk.green('Success'));

// // show the path to node; path to file to exec, show arguments
// console.log(process.argv);

// // yargs for argv parsing
// console.log(yargs.argv);
// // eg node app.js --title "things to buy"

// yargs.version('1.1.0');
// // node app.js --version %%% gives back 1.1.0
//yargs.parse()